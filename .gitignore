# Python
__pycache__/
*.pyc
*.pyo

# Old PPO model from the original tutorial run (this fork delivers DQN)
/ppo.zip

# Tensorboard logs (we use the Monitor CSV + matplotlib instead)
results/tb/

# Intermediate training checkpoints: bulky and regenerable. We commit only the
# selected best model (results/dqn_serp.zip) plus the selection history JSON.
# Covers both single-run (results/checkpoints/) and per-worker parallel runs
# (results/parallel/run_*/checkpoints/).
**/checkpoints/
